import React from 'react'
import { Button } from 'semantic-ui-react'

const AndrewsCoolButton = (props) => <Button>
    static Text
    {props.anything}
    {props.jivko}
    </Button>

export default AndrewsCoolButton